const appConfig = {
  apiPrefix: process.env.REACT_APP_API_HOST,
  authenticatedEntryPath: "/home",
  unAuthenticatedEntryPath: "/sign-in",
  tourPath: "/",
  enableMock: true,
};

export default appConfig;
